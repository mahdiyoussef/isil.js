html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/fullpost.css")
        title #{data.title}
    body
        div
            div.header
                a(href="/") 
                    p#title TechRemarkable
                a(href="/post") 
                    ul Add Post
                a(href="/logout") 
                    ul Logout
            div.post
                p.titlepost #{data.title}
                img(src=data.image, alt="facebook").imagepost
                p.desc #{data.detail}
